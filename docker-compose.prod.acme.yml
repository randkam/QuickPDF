services:
  nginx:
    # HTTP-only nginx for initial Let's Encrypt issuance.
    # Use with: docker compose -f docker-compose.prod.yml -f docker-compose.prod.acme.yml up -d nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.acme.conf:/etc/nginx/conf.d/default.conf:ro

